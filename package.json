{
  "name": "bookit-travel-booking",
  "version": "1.0.0",
  "description": "Complete travel booking application with React frontend and Node.js backend",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd server && npm install",
    "start": "cd server && npm start",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["travel", "booking", "react", "nodejs", "mongodb"],
  "author": "BookIt Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}